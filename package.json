{
  "name": "calendar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n FE,BE -c cyan,magenta \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:fe": "npm run dev --prefix frontend",
    "dev:be": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "type-check": "concurrently -n FE,BE \"npm run type-check --prefix frontend\" \"npm run type-check --prefix backend\"",
    "docs": "typedoc --lang en --out docs",
    "docs:serve": "npx serve docs --listen 3001",
    "docs:es": "typedoc --lang es --out docs-es",
    "docs:it": "typedoc --lang it --out docs-it",
    "clean": "npx rimraf frontend/dist backend/dist docs docs-es docs-it"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rimraf": "^6.0.1",
    "typedoc": "^0.28.19"
  }
}
